workspace:
  base: /go
clone:
  git:
    image: plugins/git
    depth: 1
pipeline:
  test:
    image: golang:1.10.3-alpine
    commands:
      - apk update && apk add git make
      - make deps
      - make ci
